<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.rogacheski.lbd.lbdmusic">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".MainActivity"
            android:theme="@style/AppTheme.Launcher">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".LoginActivity"
            android:label="@string/loginActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ResetActivity"
            android:label="@string/resetActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".SignupActivity"
            android:label="@string/signupActivity"
            android:theme="@style/AppTheme" />
        <activity
            android:name=".ProfileContractorActivity"
            android:label="@string/profileContractorActivity"
            android:theme="@style/AppTheme.profile" />
        <activity
            android:name=".EditfieldActivity"
            android:label="@string/profileContractorActivity"
            android:theme="@style/AppTheme.Editfield"
            android:windowSoftInputMode="stateVisible" />
        <activity
            android:name=".ProfileMusicianActivity"
            android:label="@string/title_activity_profile_musician"
            android:theme="@style/AppTheme.Transparent" />
        <activity android:name=".searchResult" />
        <activity android:name=".ContractorActivity" />
        <activity android:name=".SearchByNameActivity" />
        <activity android:name=".SearchByGenreActivity" />
        <activity android:name=".SearchByLocationActivity"></activity>
    </application>

</manifest>